rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Force deployment trigger: 2025-06-10T12:30:00Z
    // This rule allows all read and write operations for the prototyping phase.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}