rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Global allow for development and prototyping phase
    // Force deployment trigger: 2025-05-26T12:00:00Z
    match /{document=**} {
      allow read, write: if true;
    }
  }
}